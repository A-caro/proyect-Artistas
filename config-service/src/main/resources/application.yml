server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: ExamenFinal
          uri: https://github.com/A-caro/proyect-Artistas.git
          search-paths: config-data
  application:
    name: config-service

